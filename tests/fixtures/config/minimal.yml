# Config with only minimal parameter configuration
parameters:
    kernel.project_dir: /test

smart_sonata:
    parameters:
        dummy_parameter:
            value:  "some value"
    sender:
        address: 'test@test.test'
